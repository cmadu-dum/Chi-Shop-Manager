
import type { SaleCategory, ExpenseCategory } from './types';

export const SALE_CATEGORIES: SaleCategory[] = ['Service', 'Other'];
export const EXPENSE_CATEGORIES: ExpenseCategory[] = ['Supplies', 'Rent', 'Salaries', 'Utilities', 'Food', 'Other'];
export const LOW_STOCK_THRESHOLD = 5;
